mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

## Spring
spring:
  # JPA
  jpa:
    properties:
      hibernate:
        show_sql: false
  # Datasource
  datasource:
    url: jdbc:postgresql://localhost:59033/ttdbdb
    username: postgres
    password: 
    driver-class-name: org.postgresql.Driver
